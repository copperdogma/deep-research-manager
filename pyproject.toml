[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deep-research"
version = "0.1.3"
description = "CLI tool for managing multi-model deep research cycles."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "click>=8.0",
  "PyYAML>=6.0",
]

[project.optional-dependencies]
openai = [
  "openai>=1.0",
  # openai<1.55 can pass `proxies=` to httpx; httpx>=0.28 removed it.
  "httpx<0.28",
]
anthropic = ["anthropic>=0.40"]
google = ["google-generativeai>=0.8"]
# xAI uses the OpenAI-compatible client.
xai = [
  "openai>=1.0",
  "httpx<0.28",
]
all = [
  "openai>=1.0",
  "httpx<0.28",
  "anthropic>=0.40",
  "google-generativeai>=0.8",
]
dev = [
  "pytest>=8.0",
  "ruff>=0.9.0",
]

[project.scripts]
deep-research = "deep_research.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
